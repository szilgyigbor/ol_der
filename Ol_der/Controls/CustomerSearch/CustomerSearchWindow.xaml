<Window x:Class="Ol_der.Controls.CustomerSearch.CustomerSearchWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Ol_der.Controls.CustomerSearch"
        mc:Ignorable="d"
        FontSize="16"
        Title="CustomerSearchWindow" Height="700" Width="1300"
        Background="#F8FAFD">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Styles/CommonButtonStyles.xaml" />
            </ResourceDictionary.MergedDictionaries>
            <Style x:Key="ModernTextBoxStyle" TargetType="TextBox">
                <Setter Property="Background" Value="#FFFFFF"/>
                <Setter Property="Foreground" Value="#1A1A1A"/>
                <Setter Property="FontSize" Value="16"/>
                <Setter Property="Padding" Value="10,8"/>
                <Setter Property="BorderBrush" Value="#D0D7E1"/>
                <Setter Property="BorderThickness" Value="2"/>
                <Setter Property="MinHeight" Value="36"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="TextBox">
                            <Border x:Name="border"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="6"
                                    SnapsToDevicePixels="True">
                                <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsFocused" Value="True">
                                    <Setter TargetName="border" Property="BorderBrush" Value="#A8D5BA"/>
                                    <Setter TargetName="border" Property="BorderThickness" Value="2"/>
                                    <Setter TargetName="border" Property="Effect">
                                        <Setter.Value>
                                            <DropShadowEffect BlurRadius="4" ShadowDepth="0" Opacity="0.2" Color="#A8D5BA"/>
                                        </Setter.Value>
                                    </Setter>
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter TargetName="border" Property="Background" Value="#F5F5F5"/>
                                    <Setter TargetName="border" Property="BorderBrush" Value="#E0E0E0"/>
                                    <Setter Property="Foreground" Value="#A0A0A0"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>            
            <Style x:Key="ModernGridViewColumnHeaderStyle" TargetType="GridViewColumnHeader">
                <Setter Property="Background" Value="#F8FAFD"/>
                <Setter Property="Foreground" Value="#333333"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="Padding" Value="10,8"/>
                <Setter Property="BorderBrush" Value="#D0D7E1"/>
                <Setter Property="BorderThickness" Value="0,0,0,1"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="GridViewColumnHeader">
                            <Border Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    Padding="{TemplateBinding Padding}">
                                <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </ResourceDictionary>
    </Window.Resources>
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Ügyfélkeresés" FontSize="24" FontWeight="SemiBold" 
                   HorizontalAlignment="Left" Margin="0,0,0,10" Foreground="#1A1A1A"/>

        <WrapPanel Grid.Row="1" HorizontalAlignment="Left" Margin="0,0,0,20">
            <TextBlock Text="Ügyfélnév:" FontSize="16" VerticalAlignment="Center" Margin="0,0,10,0" Foreground="#333333"/>
            <TextBox Text="{Binding CustomerName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                     Style="{StaticResource ModernTextBoxStyle}" Width="300" Margin="0,0,10,0">
                <TextBox.InputBindings>
                    <KeyBinding Command="{Binding SearchCustomersByNameCommand}" Key="Enter" />
                </TextBox.InputBindings>
            </TextBox>
            <Button Content="Keresés" Style="{StaticResource CommonButtonStyle}" Command="{Binding SearchCustomersByNameCommand}"/>
        </WrapPanel>

        <WrapPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,0,15">
            <Button Content="Kiválaszt" Style="{StaticResource CommonButtonStyle}" Click="FinishSelectionClick"/>
        </WrapPanel>

        <ListView Grid.Row="3" ItemsSource="{Binding Customers}" Background="#fafafa" Foreground="#222" BorderBrush="#e0e0e0" BorderThickness="1" Margin="0 2" 
                  SelectedItem="{Binding SelectedCustomer, Mode=TwoWay}"
                  MouseDoubleClick="ListView_MouseDoubleClick">
            <ListView.View>
                <GridView ColumnHeaderContainerStyle="{StaticResource ModernGridViewColumnHeaderStyle}">
                    <GridViewColumn Header="Ügyfélnév" DisplayMemberBinding="{Binding Name}" Width="350"/>
                    <GridViewColumn Header="Cím" DisplayMemberBinding="{Binding Address}" Width="250"/>
                    <GridViewColumn Header="Telefonszám" DisplayMemberBinding="{Binding Phone}" Width="120"/>
                    <GridViewColumn Header="Emailcím" DisplayMemberBinding="{Binding Email}" Width="150"/>
                    <GridViewColumn Header="Adószám" DisplayMemberBinding="{Binding TaxNumber}" Width="150"/>
                    <GridViewColumn Header="Megjegyzés" DisplayMemberBinding="{Binding Notes}" Width="250"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Window>