<UserControl x:Class="Ol_der.Controls.Customers.ShowAllCustomerControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Ol_der.Controls.Customers"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <ListView x:Name="CustomersListView" Background="White" AlternationCount="2" ItemsSource="{Binding Customers}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Név" DisplayMemberBinding="{Binding Name}" Width="350"/>
                    <GridViewColumn Header="Cím" DisplayMemberBinding="{Binding Address}" Width="460"/>
                    <GridViewColumn Header="Telefon" DisplayMemberBinding="{Binding Phone}" Width="180"/>
                    <GridViewColumn Header="Email" DisplayMemberBinding="{Binding Email}" Width="280"/>
                    <GridViewColumn Header="Adószám" DisplayMemberBinding="{Binding TaxNumber}" Width="180"/>
                    <GridViewColumn Header="Megjegyzés" DisplayMemberBinding="{Binding Notes}" Width="300"/>
                </GridView>
            </ListView.View>
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="Background" Value="#F9F9F9"/>
                    <Setter Property="Foreground" Value="Black"/>
                </Style>
            </ListView.ItemContainerStyle>
        </ListView>

    </Grid>
</UserControl>
